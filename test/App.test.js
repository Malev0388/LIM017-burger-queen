/*import { render, screen } from '@testing-library/react';
import App from './App.js';

test('renders learn react link', () => {
  render(<App />);
  screen.debug()
  const linkElement = screen.getByText(/Hola 123/i);
  expect(linkElement).toBeInTheDocument();
});*/
